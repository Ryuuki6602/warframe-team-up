export class swapbleItemClass {
  code: number
  name: string
  icon: string

  constructor(code: number, name: string, icon: string) {
    this.code = code
    this.name = name
    this.icon = icon
  }
}
